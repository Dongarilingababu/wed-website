# Example environment variables for local development and for reference when
# adding environment variables in Render.
# Vite only exposes variables that start with VITE_ to client code.

# API endpoint used by the frontend
VITE_API_URL=https://api.example.com

# Feature flags / keys (placeholder)
VITE_FEATURE_FLAG=true
VITE_MAPS_API_KEY=your-maps-key-here

# Add any secret/server-side variables ONLY via Render dashboard (do not commit secrets)
# Example (do not commit real secrets):
# SECRET_API_KEY=super-secret-value
